pipelines {
  agent any

  stages ("build"){
      step {
        echo "paso de setp build"
      }
  }
  stages ("test"){
      step {
        echo "paso de setp test"
      }
  }
  stages ("deploy"){
      step {
        echo "paso de setp deploy"
      }
  }
}
